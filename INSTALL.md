# Инструкция по установке базы данных

## Шаг 1: Создание базы данных

База данных уже создана: `skills` (пользователь: `root`, пароль: пустой)

## Шаг 2: Инициализация структуры

### Автоматический способ (рекомендуется)

Откройте терминал в корне проекта и выполните:

```bash
php database/init.php
```

Этот скрипт автоматически:
- ✅ Создаст все необходимые таблицы
- ✅ Загрузит начальные данные тем
- ✅ Настроит индексы и связи

### Ручной способ

1. Откройте phpMyAdmin или MySQL консоль
2. Выберите базу данных `skills`
3. Перейдите на вкладку "SQL" или "Импорт"
4. Скопируйте содержимое файла `database/schema.sql`
5. Выполните SQL скрипт

## Шаг 3: Проверка установки

После инициализации проверьте:

1. **Таблицы созданы:**
   - `topics` - должно быть 15 записей
   - `user_progress` - пустая таблица
   - `study_stats` - пустая таблица
   - `users` - пустая таблица (для будущего)
   - `user_notes` - пустая таблица (для будущего)

2. **API работает:**
   Откройте в браузере: `http://localhost/skills/api/progress.php`
   
   Должен вернуться JSON:
   ```json
   {
     "success": true,
     "progress": [],
     "stats": {
       "studied": 0,
       "total": 15,
       "percentage": 0,
       "total_time": 0,
       "last_studied": null
     }
   }
   ```

## Шаг 4: Проверка в проекте

1. Откройте главную страницу проекта
2. Откройте любую тему для изучения
3. Прогресс должен автоматически сохраниться в БД
4. Обновите страницу - прогресс должен сохраниться

## Устранение проблем

### Ошибка подключения к БД

Проверьте файл `config/database.php`:
```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'skills');
define('DB_USER', 'root');
define('DB_PASS', '');
```

### Таблицы не создаются

Убедитесь, что:
- База данных `skills` существует
- Пользователь `root` имеет права на создание таблиц
- PHP расширение `pdo_mysql` установлено

Проверка расширения:
```bash
php -m | grep pdo_mysql
```

### API возвращает ошибку 500

Проверьте логи ошибок PHP:
- В OSPanel: `logs/php_error.log`
- Или включите отображение ошибок в `config/database.php`

## Дополнительно

### Резервное копирование

```bash
mysqldump -u root skills > backup.sql
```

### Восстановление

```bash
mysql -u root skills < backup.sql
```

### Очистка данных

```sql
TRUNCATE TABLE user_progress;
TRUNCATE TABLE study_stats;
```

## Готово! 🎉

После выполнения всех шагов база данных готова к использованию. Прогресс изучения будет автоматически сохраняться при открытии тем.

